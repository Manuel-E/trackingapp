<%= form_for @fleet do |f| %>
  <%= f.label :name, '' %>:
  <%= f.text_field :name %><br />
  <%= f.label :city, 'city' %>:
   <%= f.select :city_id, City.all.collect { |c| [ c.name, c.id ] }, include_blank: false %><br />

   

  <%= f.submit %>
<% end %>